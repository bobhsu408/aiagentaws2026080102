# ============================================
# 職涯導航家（CareerNav）Git 忽略規則
# ============================================

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/
*.egg
venv/
.venv/
.mypy_cache/
.pytest_cache/

# --- Node / CDK ---
node_modules/
cdk.out/
*.js.map
*.d.ts
infra/dist/

# --- Environment & Secrets ---
.env
.env.local
.env.*.local

# --- IDE ---
.vscode/
.idea/
*.swp
*.swo
*~

# --- OS ---
.DS_Store
Thumbs.db

# --- Output (scraper results, not source) ---
output/

# --- AWS ---
.aws-sam/
samconfig.toml

# --- Kiro ---
# 比賽規定：專案根目錄必須包含 /.kiro 資料夾以展示 steering / specs / hooks
# 的使用情況，且「不得將 /.kiro 資料夾或其子資料夾加入 .gitignore」。
# 因此這裡刻意不忽略 .kiro 底下任何路徑。
#
# 注意：若日後新增 .kiro/settings/mcp.json，裡面可能含 API 金鑰（如 Exa），
# 提交前務必確認金鑰是以環境變數帶入，而非硬編碼在設定檔中。

# --- AgentCore CLI 本機狀態 / 生成產物 ---
agentcore/.cli/
agentcore/cdk/dist/

# --- 臨時檔案 ---
*.log
*.tmp
*.pem
*.key
credentials*

